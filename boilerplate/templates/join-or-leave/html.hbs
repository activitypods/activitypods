<style>
    p {
        font-size: 16px;
        font-family: "Roboto", "Open Sans", "sans-serif";
        font-weight: normal;
        line-height: 19px;
    }
    .button {
        font-size: 16px;
        font-family: "Roboto", "Open Sans", "sans-serif";
        font-weight: bold;
        text-transform: uppercase;
        color: white !important;
        padding: 12px 24px;
        border-radius: 8px;
        margin-right: 10px;
        text-decoration: none;
        border: 0;
        background-color: #E2003B;
        cursor: pointer;
    }
    .greybg {
        display: inline-block;
        padding: 15px;
        background-color: #eaeaea;
    }
</style>
<body>
<div class="greybg">
    <img src="{{ frontLogo }}" alt="{{ frontName }}" style="max-width: 100%;">
    <p>
        {{user.pair:label}} s'est {{#if joined}}inscrit à{{else}}désinscrit de{{/if}} votre événement intitulé "{{event.pair:label}}"
    </p>
    <p>
        <br />
        <a href="{{eventFrontUri}}" class="button">
            Voir l'événement
        </a>
        <br />
    </p>
</div>
</body>
